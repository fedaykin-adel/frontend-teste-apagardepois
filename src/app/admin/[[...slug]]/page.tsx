"use client";
// import { ModuleProvider, moduleRegistry } from "thulhaiim";
// import moduleRegistry from "@/app/modules/module.registry";
// import { ModuleProvider } from "thulhaiim/client";
// import { scribe } from "thulhaiim";
import "../../modules/test.module";
// import { useParams } from "next/navigation";
export default function Page() {
  // const params = useParams();
  // console.log(params);
  // const slug = params.slug ?? [];
  // const moduleName = slug[0] || "default";
  // // console.log(scribe);
  // // const Module = scribe.get(moduleName);
  // if (!Module) {
  //   return <>nenhum modulo encontrado</>;
  // }
  return {
    /*<ModuleProvider module={Module}>
      <>asd</>
    {/* </ModuleProvider> */
  };
}
